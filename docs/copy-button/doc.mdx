import { Demo } from './demo'
import { Demo as DemoCustom } from './demo-custom'
import { Demo as DemoLabel } from './demo-label'

<DemoCanvas>
  <DemoPreview>
    <Demo />
  </DemoPreview>
  <DemoCode>
    ```tsx file=./demo.tsx
    ```
  </DemoCode>
</DemoCanvas>

## Installation

<InstallationTabs item="copy-button">
```tsx file=../../registry/ikui/copy-button.tsx
```
</InstallationTabs>

## Usage

```tsx
import { CopyButton } from "@/components/copy-button";
```
```tsx
<CopyButton value="Text to copy" />
```

## Examples

### Size

<DemoCanvas>
  <DemoPreview>
    <DemoCustom />
  </DemoPreview>
  <DemoCode>
    ```tsx file=./demo-custom.tsx
    ```
  </DemoCode>
</DemoCanvas>

### With label

<DemoCanvas>
  <DemoPreview>
    <DemoLabel />
  </DemoPreview>
  <DemoCode>
    ```tsx file=./demo-label.tsx
    ```
  </DemoCode>
</DemoCanvas>

## Props

<PropsTable
    data={[
      {
        name: "value",
        type: "string | (() => string | Promise<string>)",
        nameDetails: "Text to copy, or a (possibly async) function resolving to it on click"
      },
      {
        name: "size",
        type: "SizeVariant",
        default: "default",
        typeDetails: "Button size. Options: sm, default, lg"
      },
      {
        name: "timeout",
        type: "number",
        default: "1500",
        nameDetails: "How long the copied state stays before resetting, in ms"
      },
      {
        name: "copyIcon",
        type: "ReactNode",
        nameDetails: "Icon shown in the idle state"
      },
      {
        name: "copiedIcon",
        type: "ReactNode",
        nameDetails: "Icon shown after a successful copy"
      },
      {
        name: "onCopy",
        type: "(value: string) => void",
        nameDetails: "Called with the copied text after it is written to the clipboard"
      },
      {
        name: "onCopyError",
        type: "(error: unknown) => void",
        nameDetails: "Called when reading the value or writing to the clipboard fails"
      },
      {
        name: "children",
        type: "ReactNode",
        nameDetails: "Optional label rendered next to the icon, turning this into a text button"
      },
      {
        name: "copiedChildren",
        type: "ReactNode",
        nameDetails: "Label shown after a successful copy. Falls back to children when omitted"
      },
      {
        name: "className",
        type: "string",
        nameDetails: "Additional CSS classes"
      }
    ]}
  />
